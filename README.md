# CV Beton Agung Website

This contains everything you need to run your app locally.

## Project Context

Read `docs/PROJECT_CONTEXT.md` before making changes. It documents the project structure, runtime notes, data flow, and local do/don't rules.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
